# 更新日志

## 0.1.1 - 2026-06-20

- 修复仪表盘静态文件托管的路径穿越风险，并移除本地 API 的通配跨域响应头。
- 让 `trust_level` 与 `risk_weights` 真正参与风险评分和策略决策。
- 调整确认记忆优先级：永久拉黑仍优先拦截，始终允许不再覆盖明确的 `block` 策略。
- 桌面端改为自动绑定空闲本地端口，避免固定端口冲突时误打开其他本地服务。
- 更新仓库地址、作者元数据与 FAQ，并拆分前端 vendor chunk。
